# Now we pull data from production, so we don't need to seed anything here.
# The only thing we need to do is to set up the SSH keys and run rails db:pull to get the data from production.
# rails db:pull
